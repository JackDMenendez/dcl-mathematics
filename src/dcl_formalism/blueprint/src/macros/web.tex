% Macros used only by the web version. The blueprint decorations (\lean,
% \leanok, \uses, \notready, dependency graph) are provided by the
% \usepackage[dep_graph]{blueprint} plugin, so this file is usually empty.
