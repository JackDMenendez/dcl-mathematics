% automorphism_algebra.tex
% Section 4 -- automorphism algebra of the diamond progression
% in dimension d.  Extends the 3-D 71-dimensional per-site algebra
% catalogued in dcl-zoo; gives generators and commutation relations as
% a function of d, with rank closed-form.

\label{sec:automorphism_algebra}

\placeholder{Section body -- write during Phase 2.  Develop the
per-site automorphism algebra as a function of $d$; identify the
generators (rotations, reflections, parity-preserving / parity-
flipping pieces); state and prove commutation relations.}

\subsection{Generators}
\label{subsec:autom_generators}

\placeholder{Enumerate the per-site automorphism generators in
arbitrary dimension.  The hyperoctahedral group $B_d$ (signed
permutations of the $d$ coordinate axes) gives a natural set;
characterise it intrinsically rather than by appeal to the $d = 3$
enumeration.}

\subsection{Commutation Relations}
\label{subsec:autom_commutators}

\placeholder{State the commutation relations among the generators;
identify the structure constants as functions of $d$.}

\subsection{Rank and Dimension}
\label{subsec:autom_rank}

\placeholder{Closed-form expression for $\dim \mathfrak{a}_d$ as a
function of $d$.  Verify that $d = 3$ recovers the
71-dimensional algebra catalogued in \texttt{dcl-zoo}.}

\subsection{Specialisation to $d = 3$}
\label{subsec:autom_specialisation}

\placeholder{Recover the 3-D 71-dimensional per-site algebra as
the $d = 3$ instance.  Cross-reference \texttt{dcl-zoo} for the
explicit generator catalogue at $d = 3$.}
